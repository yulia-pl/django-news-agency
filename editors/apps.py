from django.apps import AppConfig


# Configuration for the Editors app
class EditorsConfig(AppConfig):
    # The default auto field type for models in this app
    default_auto_field = "django.db.models.BigAutoField"

    name = "editors"
